# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-wissem-distro"
BBFILE_PATTERN_meta-wissem-distro = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-wissem-distro = "6"

LAYERDEPENDS_meta-wissem-distro = "core"
LAYERSERIES_COMPAT_meta-wissem-distro = "walnascar"
