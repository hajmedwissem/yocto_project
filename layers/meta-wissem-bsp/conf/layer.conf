# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-wissem-bsp"
BBFILE_PATTERN_meta-wissem-bsp = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-wissem-bsp = "6"

LAYERDEPENDS_meta-wissem-bsp = "core"
LAYERSERIES_COMPAT_meta-wissem-bsp = "walnascar"
